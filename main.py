from Scheduler import Scheduler

if __name__ == "__main__":
    # sheet_name = input('Enter spreadsheet name: \n')
    # worker = input('Enter your full name: \n')
    scheduler = Scheduler('Grafik 25.03-31.03.2022', 'Kamil Styś')
    scheduler.execute()

